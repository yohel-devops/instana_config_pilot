version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: instana-config-frontend
    restart: unless-stopped
    expose:
      - "80"
    networks:
      - instana-network

networks:
  instana-network:
    driver: bridge

# Made with Bob
